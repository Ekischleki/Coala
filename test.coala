#Translational layer for problems into Graph coloring 


#Problem is a special kind of structure. Each substructure will be treated as an entry point for solving the graph
#If you want to solve it, you have to supply a solution to each substructure in main 
problem {
    sub and_solver takes(b: a, b: b) {  
        let res = b::and(a, b);
        force res => b::true;
    } 
}


solution {
    and_solver(b:true, b:true),

}